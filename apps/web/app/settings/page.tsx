import { SettingsPanel } from "@/client/SettingsPanel";

export default function SettingsPage() {
  return (
    <main className="page">
      <h1>Settings</h1>
      <SettingsPanel />
    </main>
  );
}
